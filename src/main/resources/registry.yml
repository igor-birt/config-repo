server:
  port: 8761

spring:
  application:
    name: registry
  cloud:
    service-registry:
      auto-registration:
        enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false